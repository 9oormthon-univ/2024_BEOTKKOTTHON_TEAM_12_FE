import styled from 'styled-components';

const FormGroup = styled.div`
  margin-bottom: 30px;

  .label {
    padding: 10px 0;
    font-size: 15px;
    font-weight: bold;
    color: var(--grey-7);

    .num {
      color: var(--grey-4);
    }
  }
`;

export default FormGroup;
